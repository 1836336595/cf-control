# 无刷 Crazyflie 2.1 飞行测试指南（crazyswarm + NOKOV）

> 适用环境：Ubuntu 20.04 + ROS Noetic + crazyswarm + NOKOV 动捕
> 飞机：无刷 Crazyflie 2.1（CF21BL，`radio://0/80/2M/E7E7E7E702`）
> 本文档记录"拿到源码后需要做哪些改动才能正常飞行"，以及后续如何扩展其他飞行测试。

---

## 目录

1. [环境与依赖](#一环境与依赖)
2. [拿到源码后需要做的改动](#二拿到源码后需要做的改动)
3. [编译与运行前准备](#三编译与运行前准备)
4. [飞行测试流程（悬停）](#四飞行测试流程悬停)
5. [后续其他飞行操作测试](#五后续其他飞行操作测试)
6. [常见问题排查](#六常见问题排查)
7. [安全注意事项](#七安全注意事项)

---

## 一、环境与依赖

| 依赖 | 说明 |
|------|------|
| ROS Noetic | 已安装，`source /opt/ros/noetic/setup.bash` |
| crazyswarm 源码 | 位于 `~/crazyswarm/ros_ws/src/crazyswarm` |
| **cflib ≥ 0.1.33** | 必须含 supervisor ARM API。<br>本机位于 `/home/nan/crazyflie-lib-python`（源码目录，tag 0.1.33）。<br>注意 `~/.local` 里 pip 装的 0.1.27 **没有** supervisor API，不能用 |
| NOKOV Python SDK | `nokovpy-3.0.1`（XINGYING-SDK） |
| PyYAML | 本机 6.0.3（较新版本，需配合源码里的 safe_load 修复，见下） |

**cflib 的引用方式**：运行 `cf_arm.py` 等需要 ARM 的脚本时，通过环境变量或脚本内置逻辑自动使用
`/home/nan/crazyflie-lib-python`（`cf_arm.py` 内置了 `CFLIB_PATH` 回退逻辑，无需手动设置）。

---

## 二、拿到源码后需要做的改动

### 2.1 飞机配置：`ros_ws/src/crazyswarm/launch/crazyflies.yaml`

改成自己的飞机（channel 80，id 2，对应无线地址 `radio://0/80/2M/E7E7E7E702`）：

```yaml
crazyflies:
- channel: 80
  id: 2
  initialPosition: [1.5, 1.5, 0.0]
  type: medium
```

### 2.2 动捕配置：`ros_ws/src/crazyswarm/launch/hover_swarm.launch`

把追踪源从 vicon 换成 NOKOV：

```yaml
motion_capture_type: "nokov"            # 原来是 "vicon"
object_tracking_type: "motionCapture"   # 原来是 "libobjecttracker"
motion_capture_host_name: "10.1.1.198"  # NOKOV 服务器 IP
```

### 2.3 无刷 ARM 支持（**核心改动**）

**为什么要改**：无刷 Crazyflie 2.1 起飞前必须先收到 ARM 指令电机才会转，而
`crazyswarm_server`（C++）本身不支持 ARM。

**方案**：给 `crazyswarm_server` 节点加 `launch-prefix`，在 server 连接飞机之前先用
cflib 直连发送 ARM 并等待固件确认；ARM 成功才启动 server，server 退出后自动 DISARM。

修改 `hover_swarm.launch`：

```xml
<node pkg="crazyswarm" type="crazyswarm_server" name="crazyswarm_server" output="screen"
      launch-prefix="$(find crazyswarm)/scripts/cf_arm_wrapper.sh" >
```

**新增文件**（都在 `scripts/` 下）：

| 文件 | 作用 |
|------|------|
| `cf_arm.py` | ARM/DISARM 助手：cflib 直连发送请求，通过 `supervisor.info` 日志位确认 |
| `cf_arm_wrapper.sh` | launch-prefix 包装器：ARM → 启动 server → server 退出后 DISARM |
| `hover_swarm.py` | 悬停任务脚本（起飞→悬停→降落） |

`cf_arm.py` 常用参数：

```bash
python3 scripts/cf_arm.py --arm          # ARM（手动）
python3 scripts/cf_arm.py --disarm       # DISARM（手动）
python3 scripts/cf_arm.py --arm --require-mocap   # ARM 前强制检查 NOKOV 位姿
# 其他：--uri, --timeout(默认5s 确认等待), --total-timeout(默认35s 总超时)
```

ARM 状态确认信息（`supervisor.info` 位）：
- `0x000a` = IS_ARMED + CAN_FLY → **ARM 成功，可飞行**
- `0x0001` = 仅 CAN_BE_ARMED → **已 DISARM**

### 2.4 PyYAML 兼容修复（本机 PyYAML 6.x 必需）

旧代码 `yaml.load(x)` 在 PyYAML ≥ 5.1 会报
`TypeError: load() missing 1 required positional argument: 'Loader'`。已改为 `yaml.safe_load()`：

- `scripts/pycrazyswarm/crazyflie.py`（2 处）
- `scripts/chooser.py`（2 处）

---

## 三、编译与运行前准备

```bash
# 1. 设置脚本可执行权限（新文件已设好，保险起见可再执行）
chmod +x ~/crazyswarm/ros_ws/src/crazyswarm/scripts/cf_arm.py \
         ~/crazyswarm/ros_ws/src/crazyswarm/scripts/cf_arm_wrapper.sh \
         ~/crazyswarm/ros_ws/src/crazyswarm/scripts/hover_swarm.py

# 2. 编译 crazyswarm（如之前已编译过可跳过）
cd ~/crazyswarm/ros_ws
catkin build          # 或 catkin_make

# 3. 每次新开终端都要 source 环境（注意 devel 在 ros_ws 里！）
source /opt/ros/noetic/setup.bash
source ~/crazyswarm/ros_ws/devel/setup.bash

# 4. 检查 launch 能否解析
roslaunch crazyswarm hover_swarm.launch --nodes
# 应看到 /crazyswarm_server /joy /crazyswarm_teleop /rviz
```

> 硬件检查：Crazyradio 已插 USB；飞机已上电；NOKOV 服务器在线且刚体 `cf2` 被追踪；
> 手柄接在 `/dev/input/js0`（不需要手柄也可，悬停脚本不依赖手柄）。

---

## 四、飞行测试流程（悬停）

### 步骤 1：启动 launch（自动 ARM → 启动 server）

```bash
# 终端 1
source /opt/ros/noetic/setup.bash
source ~/crazyswarm/ros_ws/devel/setup.bash
roslaunch crazyswarm hover_swarm.launch
```

预期日志：
```
连接 Crazyflie：radio://0/80/2M/E7E7E7E702
已发送ARM请求，等待固件确认……
固件已确认ARM：supervisor.info=0x000a     ← ARM 成功，server 才会继续启动
```

- 如果看到 `[cf_arm_wrapper] ARM 失败，不启动 crazyswarm_server`：
  说明 ARM 没成功（飞机没上电/无线没连上/超时），server 不会启动——这是**安全设计**。

### 步骤 2：执行悬停任务

```bash
# 终端 2（可从任意目录运行）
python3 ~/crazyswarm/ros_ws/src/crazyswarm/scripts/hover_swarm.py --height 0.8 --hold 5.0
```

常用参数：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--height` | 0.8 | 悬停高度 m（相对起飞点） |
| `--hold` | 5.0 | 悬停时间 s |
| `--takeoff-duration` | 2.5 | 起飞轨迹时间 s |
| `--land-duration` | 2.5 | 降落轨迹时间 s |
| `--land-height` | 0.04 | 降落目标高度 m |
| `--cf-index` | 0 | 使用 `allcfs.crazyflies` 下标第几架 |

### 步骤 3：结束

- 悬停脚本跑完会自动降落并停桨
- `Ctrl-C` 关闭 `roslaunch` → wrapper 会自动 DISARM
- 若被 `kill -9` 强杀没自动 DISARM，手动执行：
  ```bash
  python3 ~/crazyswarm/ros_ws/src/crazyswarm/scripts/cf_arm.py --disarm
  ```

---

## 五、后续其他飞行操作测试

### 5.1 用 pycrazyswarm 写新脚本（推荐，配合本套 launch）

模板 `scripts/my_flight_test.py`：

```python
#!/usr/bin/env python3
"""自定义飞行测试脚本模板"""
import os
from pycrazyswarm import Crazyswarm

# 复用悬停脚本的绝对路径写法，保证任意目录可运行
CRAZYFLIES_YAML = os.path.join(
    os.path.dirname(os.path.abspath(__file__)), "..", "launch", "crazyflies.yaml"
)

def main():
    swarm = Crazyswarm(crazyflies_yaml=CRAZYFLIES_YAML)
    timeHelper = swarm.timeHelper
    cf = swarm.allcfs.crazyflies[0]

    # 起飞
    cf.takeoff(targetHeight=0.8, duration=2.5)
    timeHelper.sleep(3.5)

    # 飞到目标点（世界坐标）
    cf.goTo([0.0, 0.0, 0.8], yaw=0.0, duration=3.0)
    timeHelper.sleep(4.0)

    # 或直接给位置设定点
    cf.cmdPosition([0.5, 0.0, 0.8], yaw=0.0)
    timeHelper.sleep(2.0)

    # 降落
    cf.land(targetHeight=0.04, duration=2.5)
    timeHelper.sleep(3.0)

if __name__ == "__main__":
    main()
```

运行方式（server 已通过 `roslaunch hover_swarm.launch` 启动、飞机已 ARM）：

```bash
chmod +x ~/crazyswarm/ros_ws/src/crazyswarm/scripts/my_flight_test.py
python3 ~/crazyswarm/ros_ws/src/crazyswarm/scripts/my_flight_test.py
```

**pycrazyswarm 常用 API**（`cf = swarm.allcfs.crazyflies[0]`）：

| 方法 | 说明 |
|------|------|
| `cf.takeoff(height, duration)` | 垂直起飞到相对高度 |
| `cf.land(height, duration)` | 降落 |
| `cf.goTo([x,y,z], yaw, duration, relative=False)` | 飞往目标点 |
| `cf.cmdPosition([x,y,z], yaw)` | 位置设定点（持续指令） |
| `cf.cmdVelocityWorld([vx,vy,vz], yawRate)` | 世界系速度设定点 |
| `cf.cmdFullState(pos, vel, acc, yaw, omega)` | 全状态设定点（高级） |
| `cf.uploadTrajectory(id, pieceOffset, trajectory)` + `cf.startTrajectory(id)` | 上传并执行轨迹 |
| `swarm.allcfs.takeoff(...)` / `land(...)` / `goTo(...)` | 多机编队命令（作用于所有飞机） |

### 5.2 现成脚本可直接参考/运行

**crazyswarm 自带示例**（`scripts/` 下，用 pycrazyswarm 写的）：

- `hello_world.py` — 单机起飞悬停降落（最简单）
- `cmdVelocityCircle.py` / `cmdFullState.py` — 速度/全状态指令示例
- `waypoints.py` / `waypoints_simple.py` — 航点飞行
- `figure8_csv.py` / `csv_sequence.py` — 轨迹文件飞行

> 注意：这些自带脚本用 `Crazyswarm()` 默认相对路径加载 yaml，**需在 `scripts/` 目录下运行**，
> 或按 5.1 模板改成绝对路径。

**直接 cflib 控制脚本**（不经 ROS，单独运行，含完整 ARM/安全检查，在 `~/catkin_ws/src/`）：

| 脚本 | 功能 |
|------|------|
| `nokov_crazyflie_control.py` | 单机悬停/定点飞行（`hover`、`goto` 子命令），直接 cflib + NOKOV |
| `nokov_circle_flight.py` | 三维圆形轨迹飞行（参数丰富，含预览模式 `--preview-only`） |

这些脚本自带 ARM/DISARM 流程，需要 `--arm` 参数才会启动电机，适合不依赖 ROS 的测试。

### 5.3 常见参数调整点

- **控制器**：`hover_swarm.launch` 里 `stabilizer.controller: 2` 是 Mellinger；想用 PID 改成 `1`
- **起飞/悬停高度**：`hover_swarm.py --height` / `--hold`
- **动捕原点/刚体**：`cf_arm.py --server-ip --rigid-body`（默认 10.1.1.198 / cf2），
  `hover_swarm.launch` 里 `motion_capture_host_name`
- **飞机无线地址**：换飞机时改 `crazyflies.yaml`（channel/id）和 `cf_arm.py` 里的
  `DEFAULT_URI`（`radio://0/{channel}/2M/E7E7E7E7{id:02X}`）
- **ARM 前强制检查 NOKOV**：wrapper 里给 `cf_arm.py` 加 `--require-mocap`

---

## 六、常见问题排查

| 现象 | 原因 | 解决 |
|------|------|------|
| `Cannot find a Crazyradio Dongle` | 无线接收器没插/驱动没加载 | 插好 Crazyradio 再试 |
| `'NoneType' object has no attribute 'link'` | 旧 bug（LogConfig 未先 add_config）| 已修复；如再出现检查 cflib 版本 |
| `yaml.load() missing ... 'Loader'` | PyYAML 太新 | 已修复为 safe_load |
| `FileNotFoundError: '../launch/crazyflies.yaml'` | 相对路径问题 | 用 hover_swarm.py（已改绝对路径）或 cd 到 scripts 目录 |
| ARM 失败 `[cf_arm_wrapper] ...` | 飞机没上电/无线没连上/超时 | 检查电源、无线；看 `--total-timeout` |
| 悬停脚本卡住等待 | 没先启动 server | 先 `roslaunch hover_swarm.launch` |
| `supervisor.info` 没有 | 固件太旧 | 该机为 legacy 固件（协议 v8），走 platform 回退，功能正常 |

---

## 七、安全注意事项

1. **首次测试务必卸下螺旋桨**，先在 `cf_arm.py` 上用 `--disarm`/`--arm` 验证逻辑
2. ARM 会让电机**怠速转动**，属正常现象；确保飞机放置平稳、远离人员
3. 起飞前确认 NOKOV 追踪正常（rviz 里能看到飞机位姿），否则别起飞
4. `Ctrl-C` 结束 roslaunch 会自动 DISARM；被强杀后记得手动 `cf_arm.py --disarm`
5. 高度、悬停时间从小开始（如 `--height 0.5 --hold 3.0`），确认稳定后再加大
